// Use modes
bool USEGLOVE = true;
bool HIRES_DATA = false;

// Glove variables
char* glove_port = "{port}";
int baudRate = 115200;
int rawSenor_n = 22;
bool updateRawRange = false; // Update the range if values outside range is reported

// Hand Model
char* logFile = "{logfile}"; // "none" for no logs
int calibSenor_n = 24;

// Calibration
char* calibFile =     "{calibfile}";
char* userRangeFile = "{userrangefile}";
char* handRangeFile = "{handrangefile}";
